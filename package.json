{
  "name": "idb-simple",
  "version": "0.0.1",
  "description": "Implement a simple key-value storage for indexedDB on top and add some helper methods such as for storing images",
  "main": "index.js",
  "scripts": {
    "start": "browserify index.js -o dist/index.js",
    "compile": "watchify index.js -o dist/index.js",
    "start-example": "watchify example/index.js -o example/dist.js"
  },
  "keywords": [
    "indexedDB",
    "storage",
    "node.js",
    "browser"
  ],
  "author": "Carlos Galarza <carloslfu@gmail.com>",
  "license": "MIT",
  "dependencies": {
    "idb": "^2.0.3"
  },
  "devDependencies": {
    "browserify": "^14.3.0",
    "watchify": "^3.9.0"
  }
}
